/** Display labels for verb.pattern_behavior values (MVP: English UI). */
export const PATTERN_BEHAVIOR_LABELS = {
  only_gerund: "Only + -ing",
  only_infinitive: "Only + to + verb",
  both_change: "Both (-ing and to; meaning can change)",
};
